module garb::sim {
    use aptos_framework::aptos_coin::AptosCoin;
    use aptos_framework::coin::Coin;

    entry public fun try_route<C1, C2, C3, C4, C5, C7, E1, E2, E3, E4, E5>(route_providers: vector<u8>, routes_count: u8, step_outputs: vector<address>) {
        if (routes_count == 1) {

        }
    }

    fun find_route() {

    }
}